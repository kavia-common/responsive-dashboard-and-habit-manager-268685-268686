import React from 'react';

// PUBLIC_INTERFACE
export function CalendarPage() {
    /** Calendar view page (placeholder). */
    return (
        <div>
            <h1 className="pageTitle">Calendar</h1>
            <p className="pageHint">See check-ins by day and review history.</p>

            <div className="card">
                <div className="cardTitle">Calendar</div>
                <div className="mutedText">Calendar component will be added in the next step.</div>
            </div>
        </div>
    );
}
