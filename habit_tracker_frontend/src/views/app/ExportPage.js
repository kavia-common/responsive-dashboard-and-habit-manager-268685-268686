import React from 'react';

// PUBLIC_INTERFACE
export function ExportPage() {
    /** Data export page (placeholder). */
    return (
        <div>
            <h1 className="pageTitle">Export</h1>
            <p className="pageHint">Download your data in common formats.</p>

            <div className="card">
                <div className="cardTitle">Export options</div>
                <div className="mutedText">Export logic (CSV/JSON) will be implemented in the next step.</div>
            </div>
        </div>
    );
}
