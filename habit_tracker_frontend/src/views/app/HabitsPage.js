import React from 'react';

// PUBLIC_INTERFACE
export function HabitsPage() {
    /** Habits CRUD page (placeholder). */
    return (
        <div>
            <h1 className="pageTitle">Habits</h1>
            <p className="pageHint">Create, edit, and manage your habit schedules and goals.</p>

            <div className="card">
                <div className="cardTitle">Habits list</div>
                <div className="mutedText">CRUD UI and modal editing will be implemented in the next step.</div>
            </div>
        </div>
    );
}
