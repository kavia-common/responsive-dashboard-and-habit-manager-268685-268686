import React from 'react';

// PUBLIC_INTERFACE
export function RemindersPage() {
    /** Reminders preferences and management page (placeholder). */
    return (
        <div>
            <h1 className="pageTitle">Reminders</h1>
            <p className="pageHint">Configure reminder preferences and manage schedules.</p>

            <div className="card">
                <div className="cardTitle">Reminder settings</div>
                <div className="mutedText">Reminder management UI will be implemented in the next step.</div>
            </div>
        </div>
    );
}
