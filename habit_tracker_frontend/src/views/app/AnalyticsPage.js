import React from 'react';

// PUBLIC_INTERFACE
export function AnalyticsPage() {
    /** Analytics dashboard with charts (placeholder). */
    return (
        <div>
            <h1 className="pageTitle">Analytics</h1>
            <p className="pageHint">Progress charts and trends across habits.</p>

            <div className="card">
                <div className="cardTitle">Charts</div>
                <div className="mutedText">Charting integration will be added in the next step.</div>
            </div>
        </div>
    );
}
